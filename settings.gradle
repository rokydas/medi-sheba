pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        flatDir {
            dirs "libs"
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url('https://repo1.maven.org/maven2/') }
        flatDir {
            dirs "libs"
        }
    }
}
rootProject.name = "medi_sheba"
include ':app'
